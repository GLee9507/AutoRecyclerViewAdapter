package com.glee.autorecyclerviewadapter.core;

public class Header extends Hf{
    public Header(String key, int layoutId, int brId, Object data) {
        super(key, layoutId, brId, data);
    }

    public Header(String key, int layoutId, int brId) {
        super(key, layoutId, brId);
    }
}
