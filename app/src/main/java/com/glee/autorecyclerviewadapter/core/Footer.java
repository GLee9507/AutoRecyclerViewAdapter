package com.glee.autorecyclerviewadapter.core;

public class Footer extends Hf{
    public Footer(String key, int layoutId, int brId, Object data) {
        super(key, layoutId, brId, data);
    }

    public Footer(String key, int layoutId, int brId) {
        super(key, layoutId, brId);
    }
}
